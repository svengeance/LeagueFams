{
  "name": "leaguefams-backend",
  "version": "0.2.0",
  "description": "Node backend for LeagueFams Riot mutual-match analysis",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/riotClient.js && node --check src/analyzeService.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.21.2"
  }
}
